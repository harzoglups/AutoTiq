# AutoTiq - Environment Variables Example
# Copy to .env and customize if needed

# Java Home (for Gradle builds)
JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"

# Android SDK paths
ANDROID_HOME="$HOME/Library/Android/sdk"
ADB="$HOME/Library/Android/sdk/platform-tools/adb"

# Fairtiq package name
FAIRTIQ_PACKAGE="com.fairtiq.android"

# Default settings for testing
DEFAULT_CHECK_INTERVAL_SECONDS=30
DEFAULT_PROXIMITY_DISTANCE_METERS=200

# VPS Deployment Configuration
# Only VPS_USER needs to be configured (SSH username)
# Other values (host, paths, URLs) are public and hardcoded in scripts
VPS_USER="your-username"
